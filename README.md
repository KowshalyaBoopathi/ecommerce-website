# ecommerce-website

# Adding extensions
1) Add the extensions Playwright and CucumberJS Test extension and Cucumber for visual studio code

# installation guide
1) node js - check with node -v and npm -v
2) npm install (based on the dependencies in package.json, all dependecies will be installed)
3) 

# .env folder
1) create a .env folder to give the credentials and url

# command to run
npx cucumber-js --tags "@login"